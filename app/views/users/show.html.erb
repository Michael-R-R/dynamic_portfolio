<% provide(:title, @user.username)%>

<div class="v-container">
  <div class="v-container-item">
    <% if logged_in? %>
      <h1><%= @user.username %></h1>
      <h1>Logged in</h1>
    <% end %>
  </div>
</div>