// Gets the count of: show_count
// Gets the count of: max_count
// If show_count < max_count then fetch 3 more entries
// append all 3 entries into html file

let show_count = document.getElementById("show-count");
let max_count = document.getElementById("max-count");

console.log(parseInt(show_count.value))
console.log(parseInt(max_count.value))
